
{% block head %}
{% include "_base.twig" %}
{% endblock %}

{% block body %}
{% block nav%}
    {% include "partials/nav.twig" %}
{% endblock %}
{% block contents%}
    <div class="container">
    <h1>{{ page_h1 }}</h1>
        <h5>{{ base_uri }} {{ baseurl }}</h5>

        <p>{{ content | raw }}</p>
        <div class="card" style="padding: 10px">
            <div class="card-body row">
                <form id="form-start" name="form-start" action="start" method="post">
                    -<div class="form-group row">
                        <div class="col-sm-6" style="padding: 10px">Click on <button>Start</button> button to create a <b>Checkout</b> session for redeeming
                        group of QR tickets. You will be able to stop any time and print the results as a <b>Receipt</b></div>
                    </div>
                    <!--<div class="form-group row">
                        <label for="Name" class="col-sm-9 col-form-label">Campaign Title *</label>
                        <div class="col-sm-12">
                            <select id="print" name="campaign" class="print form-control">
                                {% for campaign in campagins %}
                                <option  value="{{ campaign.id }}">{{ campaign.title|capitalize }}</option>
                                {% endfor %}

                            </select>
                        </div>

                    </div>-->
                    <!--<div class="form-group row">
                        <label for="r_id" class="col-sm-9 col-form-label">r_id</label>
                        <div class="col-sm-12">
                           <input type="text"  class="form-control" id="r_id" name="r_id" placeholder="random#">

                        </div>

                    </div>-->
                    <div class="form-group row">
                        <div class="col-sm-12">
                            <input type="hidden"  readonly class="form-control" id="redeem_id" name="redeem_id" value="{{ time }}">

                        </div>
                        <label for="start_date" class="col-sm-9 col-form-label">Timestamp</label>
                        <div class="col-sm-12">
                            <input type="text"  readonly class="form-control" id="redeem_date" name="redeem_date" value="{{ now }}">

                        </div>

                    </div>

                    <input type="submit" class="btn btn-danger btn-lg mb-2 print" id="start" name="submit" value="Start"/>


                </form>
            </div>
        </div>

    </div>
{% endblock %}
{% endblock %}
