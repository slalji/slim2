
{% block head %}
{% include "_base.twig" %}
{% endblock %}

{% block body %}

    {% block contents %}
        <div class="container">

            <div class="card-header">
                {% include "partials/nav.twig" %}
                <span style="color:green"><h1>{{ page_h1 }}</h1></span>
                Reports for user {{ admin }}
            </div>

        <div class="clear"><p></p></div>
        <div class="form-group row">
            <div class="col-sm-12" >

                <div class="card">
                    <div class="card-body row">
                        <form id="form-search" name="form-search" action="./{{ cid }}/results" method="get">
                            <div class="form-group ">
                                <label for="Name" class="col-sm-12 col-form-label">List of Reports  {{ title }}</label>
                                <div class="col-sm-12">


                                        {% for key,value in content %}
                                            <p>Report :  <a href="{{ value }}">{{ key }}</a>

                                        {% endfor %}



                                </div>

                                <!--
                                <label>{{ title }}</label>
                                <table><th>Title</th>
                                    <th>id</th>
                                    <th>Total</th>
                                    <th>Date</th>
                                    <th>Receipt</th>
                                    <th>Mobile</th>
                                    <th>Name</th>
                                    <th>Comment</th>

                                    {% for item in content %}
                                    <tr>
                                        {% for key,value in item %}
                                            {% if key =='total' %}
                                                <td>{{ value  |number_format(0, '.', ',') }}</td>
                                            {% else %}
                                                <td>{{ value }}</td>
                                            {% endif %}
                                    {% endfor %}
                                    </tr>
                                {% endfor %}
                                </table>
                                -->
                            </div>

                        </form>


                    </div>
                    <form action="./search/{{cid}}" method="post">
                                <label for="Search" class="col-sm-12 col-form-label">Search Receipt or QR-Code</label>
                                <input type="text" required name="needle">
                                 <input type="hidden" name="cid" id="cid" value="{{ cid }}">
                                <input type="submit" name="submit">
                                </form>
                </div>
            </div>
        </div>
        </div>


<style>
    .col-xs-4{
        padding:10px;
        xmargin:10px;
        border-left:1px #ccc solid;
        xwidth:200px;
    }
    th{
        font-weight: bold;
        text-align: center;
        background:#ccc;
        border:2px solid;
    }
    td{
        padding:10px;
        margin:10px;
        border:1px solid #eee
    }
</style>


                            </div>
        </div>
        </div>

    {% endblock %}
{% endblock %}
